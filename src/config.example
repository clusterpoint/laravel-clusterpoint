<?php

/*
|--------------------------------------------------------------------------
| API Connections config file
|--------------------------------------------------------------------------
|
| You can choose connection by passing the right connection name as
| a string on itinalizing API. Host, Account ID, Username and Password are
| reqired fields. Debug value is false if it is not set by default. 
| 
| If Debug mode is on, API will "echo" every error catched during your
| work flow with Clusterpoint PHP API.
|
| EU host : "https://api-eu.clusterpoint.com/v4"
| US host : "https://api-us.clusterpoint.com/v4"
|
*/

return array(
    'default' => array(
        'host' => env('CP_HOST', 'https://api-eu.clusterpoint.com/v4'),
        'account_id'  => env('CP_ID', ''),
        'username'=> env('CP_USERNAME', ''),
        'password'  => env('CP_PASSWORD', ''),
        'debug'  => false
    )
);
